<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Dashboard</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;700&display=swap" rel="stylesheet" />

  <style>
    body {
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      background: #f0f2f5;
    }

    .dashboard-wrapper {
      padding: 40px;
      margin: 50px auto 0 auto;
      min-height: 80vh;
      max-width: 1500px;
      width: 90%;
      /* background: linear-gradient(to right, #f3f4f7, #eef2f7); */
      border-radius: 20px;
    }

    .welcome-header {
      font-size: 38px;
      font-weight: 600;
      color: #2c3e50;
      margin-bottom: 50px;
      text-align: left;
      padding-left: 20px;
      font-family: 'Poppins', sans-serif;
      animation: fadeInDown 0.8s ease;
      position: relative;
    }

    .welcome-header .admin-name {
      font-weight: 700;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #007bff, #00c6ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .welcome-header .emoji {
      animation: wave 1.6s infinite;
      display: inline-block;
      transform-origin: 70% 70%;
    }

    .welcome-header::after {
      content: "";
      display: block;
      width: 60px;
      height: 3px;
      background: #007bff;
      margin: 10px 0 0 20px;
      border-radius: 3px;
    }

    @keyframes wave {
      0% { transform: rotate(0deg); }
      15% { transform: rotate(14deg); }
      30% { transform: rotate(-8deg); }
      45% { transform: rotate(14deg); }
      60% { transform: rotate(-4deg); }
      75% { transform: rotate(10deg); }
      100% { transform: rotate(0deg); }
    }

    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .card-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 40px;
    }

    .dashboard-card {
      width: 320px;
      height: 220px;
      border-radius: 20px;
      background: #fff;
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.08);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      cursor: pointer;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .dashboard-card:hover {
      transform: translateY(-10px) scale(1.02);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
      background: linear-gradient(to top right, #e0f0ff, #ffffff);
    }

    .dashboard-card .card-body {
      text-align: center;
    }

    .dashboard-icon {
      font-size: 60px;
      margin-bottom: 20px;
      color: #007bff;
      transition: color 0.3s ease;
    }

    .dashboard-card:hover .dashboard-icon {
      color: #0056b3;
    }

    .dashboard-card h4 {
      font-size: 24px;
      font-weight: 700;
      margin-bottom: 12px;
      color: #2c3e50;
    }

    .dashboard-card p {
      font-size: 15px;
      color: #666;
      margin: 0;
    }
  </style>
</head>

<body>

  <div class="dashboard-wrapper">
    <div class="welcome-header">
      Welcome, <span class="admin-name">Admin</span> <span class="emoji">👋</span>
    </div>

    <div class="card-container">

      <div class="card dashboard-card">
        <div class="card-body">
          <div class="dashboard-icon">
            <i class="bi bi-people-fill"></i>
          </div>
          <h4>Total Students (<%= typeof totalStudents === 'undefined' ? 0 : totalStudents %>)</h4>
          <p>View and manage student records.</p>
        </div>
      </div>

      <div class="card dashboard-card">
        <div class="card-body">
          <div class="dashboard-icon">
            <i class="bi bi-book-half"></i>
          </div>
          <h4>Total Books (<%= typeof totalBooks === 'undefined' ? 0 : totalBooks %>)</h4>
          <p>Check inventory of all books.</p>
        </div>
      </div>

      <div class="card dashboard-card">
        <div class="card-body">
          <div class="dashboard-icon">
            <i class="bi bi-journal-check"></i>
          </div>
          <h4>Issued Books (<%= typeof issuedBooks === 'undefined' ? 0 : issuedBooks %>)</h4>
          <p>Monitor all issued and returned books.</p>
        </div>
      </div>

    </div>
  </div>

</body>
 
</html>
